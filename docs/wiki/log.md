# Wiki Log

Append-only. Do not edit or delete past entries — correct forward with a new entry instead. Format: `## [YYYY-MM-DD] <op> | <title>`, where `<op>` is `ingest` (a new raw source was decomposed into the wiki) or `query` (a synthesized answer was filed back as a new/updated page).

## [2026-08-16] ingest | docs/design.md — Autonomous Agentic Development System: Brainstorming & Research

Decomposed into 15 pages under `architecture/`: overview, tech-stack, presence-detection, tracker-adapter, agent-handoff, harness-dispatch, harness-tracker-isolation, pm-scope, research-agent, dedup-death-loop, symphony-patterns, state-machine-gaps, review-rework-ux, observability, error-stall-visibility. The "Next steps" checklist section was deliberately left un-ingested (see `index.md` § Not yet ingested) — it's a living TODO, not stable knowledge.

## [2026-08-16] ingest | docs/research.md — Autonomous Coding-Agent Orchestration and the Proactive-PM Question: Research Findings

Decomposed into 7 pages under `research/`: prior-art-landscape, pm-layer-novelty, practitioner-reality, risks-and-critiques, matplotlib-incident (split out as its own page — cross-referenced from multiple architecture pages, warranted standalone treatment), presence-automation-prior-art, open-questions. The full sources list (~70 citations) was not split into a dedicated `sources.md` — each page cites its own load-bearing sources inline instead; the raw `docs/research.md` § Sources remains the exhaustive citation list if a full audit is ever needed.

## [2026-08-16] query | design.md's un-ingested "Next steps" checklist was stale

The `lucid` repo's initial scaffold (`docs/FEATURES.md`, `docs/CLI.md`, the Rust skeleton) resolved or superseded most of `docs/design.md`'s "Next steps" list without that section ever being updated to say so. Corrected the section in place (marked done/superseded/still-open per item) and pointed future maintenance at `docs/FEATURES.md` § Deferred / not v1 instead, to stop two open-items lists drifting apart. This is the exception the "immutable raw source" rule already carved out — that section was deliberately excluded from ingestion for exactly this reason (it churns faster than wiki pages should); editing it is consistent with that, not a violation of it. No wiki pages needed changes as a result — the correction was scoped to that one section of the raw source.

## [2026-08-16] query | Rust toolchain/dependency currency audit; `PresenceSource` made async

Full dep audit (all 12 pinned crates confirmed at registry-latest via `cargo info` + `cargo update --dry-run`; none stale or hallucinated). Applied: `[lints]` table (`unsafe_code = "forbid"`, `clippy::pedantic = "warn"`) and `rust-version = "1.87"` (zbus's MSRV, the highest floor among direct deps) added to `Cargo.toml`; `reqwest` features widened to keep `http2`/`system-proxy` after disabling default-features for the rustls swap; `WorkerRun.last_event_at` changed from `std::time::Instant` to `chrono::DateTime<Utc>` (`Instant` can't serialize and isn't wall-clock, and this field is headed for SQLite). Design question surfaced and resolved with the user: `PresenceSource` was sync-signature over an inherently-async D-Bus read; changed to `async fn` (`async-trait`, matching `TrackerAdapter`'s existing pattern) rather than a sync-with-background-cache shim — rationale filed to `architecture/presence-detection.md`.

## [2026-08-17] query | Trace correlation: tagging harness dispatches with OTel resource attributes

Filed new page `architecture/trace-correlation.md`, closing a concrete instance of the [proof-of-work artifacts](architecture/observability.md#proof-of-work-artifacts) gap: use the standard `OTEL_RESOURCE_ATTRIBUTES` env var to tag each dispatched harness subprocess with `ticket_id`/`dispatch_id` (both already-existing orchestrator state, no new IDs), then post a trace-query link back to the tracker item via the tracker adapter. Confirmed both Claude Code and Codex ship built-in OpenTelemetry support (no plugin needed) — Claude Code purely env-var driven, Codex TOML-config-driven with an env-var asymmetry noted on the page. Also recorded the local trace-backend choice: Arize Phoenix (OSS, OTLP-native ingest, SQLite-by-default storage, single Docker container) as the observability/eval sink; the `lucid` binary itself stays undockerized. Cross-linked from `observability.md`; `docker-compose.yml` added at repo root for the Phoenix service.

## [2026-08-17] query | Retired `docs/design.md` and `docs/research.md`; wiki is now the sole design record

Both raw sources had been fully decomposed into the wiki (all 22 pages) with nothing left un-ingested besides `docs/design.md`'s already-corrected "Next steps" section (see the [2026-08-16] entry above). Keeping them present but frozen no longer served a purpose: they weren't external sources of truth needing preservation for citation, just internal brainstorming that had already done its one job (seeding the wiki). Deleted both files (recoverable via `git log` on this repo if ever needed) and dropped the "immutable raw source" framing from the root `CLAUDE.md` — the wiki is now edited directly as understanding changes, with no frozen layer underneath it. Removed the per-page `Source: docs/design.md ...` citation lines across the wiki (they cited an internal doc, not an external fact) while keeping genuine external citations in place (e.g. `research/matplotlib-incident.md`'s primary sources). Updated `README.md`, `docs/FEATURES.md`, and `docs/CLI.md` to point at the relevant wiki pages instead of the now-deleted files or dangling `decision #N` numbers.

## [2026-08-17] query | MVP e2e proven live against `claude -p` subscription auth; `LinearAdapter` implemented

`research-first`-grounded the actual requirements for a working `claude -p` dispatch (docs checked against Claude Code v2.1.23x): headless `-p` sessions always start in Manual permission mode regardless of plan tier (the "Pro/Max/Team defaults to auto mode" behavior is interactive-only), so dispatch was hanging on the first tool call with no flags set. Fixed by passing `--permission-mode auto` explicitly — confirmed to work in non-interactive sessions (a classifier reviews actions instead of skipping review outright, and repeated classifier blocks are documented to skip that action and continue rather than hang, unlike `bypassPermissions`, which Anthropic's own docs scope to "isolated containers/VMs only" and which lucid's git-worktree isolation doesn't literally satisfy). Also corrected block detection: the `rate_limit`/`billing_error`/`oauth_org_not_allowed` signals only ever appear inside `--output-format stream-json` event JSON (the `system/api_retry` event's `error` field), never as literal substrings in plain-text output — `harness::detect_block`'s original substring-scan implementation was replaced with real NDJSON event parsing. `harness::dispatch_with_fallback` also gained `.current_dir(workdir)` (previously unset) and now surfaces the harness's own `session_id`/`is_error`/`result` text from the stream, not just exit status.

Built `tracker::file::FileTracker`, a JSON-file-backed `TrackerAdapter`, so the full dispatch → trace-link → tracker-note loop could be proven without needing Linear credentials yet. Result: `cargo run --example e2e_smoke` (new, gated out of `cargo test` — it hits the real `claude` binary) ran a real subscription-authenticated `claude -p` turn end to end, captured a real session id and dispatch id, and posted the trace-link note to the file tracker. Split `src/main.rs` into a `lucid` lib + thin bin so the example could import the crate's modules.

In parallel, a background agent implemented `LinearAdapter` for real (previously all `todo!()`), grounded directly against Linear's published GraphQL SDK schema rather than from memory — full findings filed to [tracker-adapter.md § `LinearAdapter` implementation notes](architecture/tracker-adapter.md#linearadapter-implementation-notes). Notable: `issueCreate` requires a team id resolved from a short team key Linear's own trait design hadn't accounted for, so `TrackerConfig` gained a `team_key: Option<String>` field rather than changing `TrackerAdapter`'s trait signature; the env var is `LINEAR_API_KEY`.

Net: `cargo build`/`cargo test` (19 passing)/`cargo clippy --all-targets` all clean; the only remaining `todo!()`s are `main.rs`'s daemon-loop handlers (config loading is real now, but nothing calls `Config::load` or wires `FileTracker`/`LinearAdapter` selection together yet) and the reconciliation loop itself (docs/FEATURES.md § Reconciliation loop) — the next real chunk of scope.

## [2026-08-17] query | Reconciliation loop, presence override, and PM wake implemented; `lucid start` is real

Closed the gap from the previous entry: built the actual daemon loop. `Config::load` now really parses TOML (`src/config.rs`, new `DaemonConfig` for tick/timeout/workdir knobs); `tracker::build` selects `FileTracker` vs `LinearAdapter` from `TrackerConfig.backend`. Presence gained an explicit-override layer (`src/presence/override_file.rs`) deliberately *not* implemented as a `PresenceSource` — it's a priority short-circuit (`Active`/`Autonomous` bypass the debounce entirely), not a composable idle signal, which the original design language ("always wins over automatic sources") implied but didn't state precisely; `presence::resolve` is the actual mode-resolution function combining override + sustained-idle debounce. Since `logind`'s real D-Bus wiring is still unimplemented (and known-broken on WSL2 regardless), the daemon runs with an **empty** automatic-source list by default — a deliberate conservative choice (documented at `main.rs::default_presence_sources`) so autonomy today only ever comes from an explicit `lucid presence override autonomous`, not a silent crash from calling an unimplemented source's `is_idle()`.

Built `src/pm.rs` (PM gap-detection wake): dispatches a restricted `--allowedTools` (read-only-ish) harness call, parses a JSON-array response into `Proposal`s, dedups via `query_similar`, files via `create_proposal`, respects a cap and `--dry-run`. Built `src/daemon.rs` (`Daemon::run_foreground`): presence-gated tick loop — dispatch newly-approved issues (retrying `Failed`/`TimedOut`, leaving `Succeeded` alone), then a PM wake once its interval has elapsed. Deliberately **sequential per tick, not a concurrent task-supervisor** — a real spawn-and-poll-across-ticks model was judged more machinery than this pass warranted; stall protection is per-dispatch-timeout, not a separate concurrent stall detector. State is in-memory only (`Daemon.runs`), consistent with `rusqlite` persistence being a separately-deferred item, not silently dropped.

`harness::dispatch_with_fallback` gained a hard timeout + `kill_on_drop(true)` and a `DispatchRequest`/`DispatchError` (`thiserror`) shape — not in the original design, added because an unbounded `cmd.output()` would hang the whole reconciliation tick forever on a stalled harness, which is exactly the dominant real-world failure mode the research pass already flagged. `worker::run_dispatch` and `pm::wake` both go through this; a Worker dispatches under `--permission-mode auto` (full access, classifier-reviewed), a PM wake under an explicit `--allowedTools` read-only list — two different callers needing genuinely different permission surfaces was the reason `DispatchRequest` bundles `claude_extra_args` instead of a single hardcoded mode.

Live-tested the full loop against real `claude -p` subscription auth (not just unit tests): seeded a `proposal:approved` issue in a `FileTracker` store, ran `lucid start --foreground`, watched it dispatch once, mark `Succeeded`, post the trace-link note, and correctly *not* re-dispatch on later ticks. That same live run caught a real bug — PM wake's `last_pm_wake` timestamp was only updated on success, so a failing wake (empty scratch workdir, no git repo to investigate) retried every 5-second tick forever instead of backing off; fixed by recording the attempt before the dispatch regardless of outcome, verified live afterward (one attempt, then correct backoff).

`src/main.rs` fully wired: `start` (foreground; errors clearly on `--foreground`-less detached mode, matching docs/CLI.md's own "not yet designed" admission rather than pretending to support it), `pm wake`, `presence status`/`override`, `config validate`/`show` are all real. `status`/`show`/`stop` remain unimplemented — genuinely blocked on IPC design docs/CLI.md already flags as undecided, not descoped silently. `docs/FEATURES.md` updated checkbox-by-checkbox against what's actually built, including items intentionally not yet done. `cargo build`/`cargo test` (36 passing)/`cargo clippy --all-targets` all clean.

## [2026-08-17] query | Worker completion: dispatch prompt was title-only, tracker never closed a task, commit/review policy designed and built

Prompted by "can this be used e2e right now, and how does a Worker actually complete a task" — the honest answer on both counts was worse than expected, and both were real bugs, not just missing scope:

1. **`TrackerIssue` never carried a description at all**, and `daemon::dispatch_approved_issues` passed only `issue.title` to `worker::run_dispatch` — the frontmatter+body handoff surface [Agent handoff](architecture/agent-handoff.md) describes existed in `render_description`'s output but never reached the dispatched harness. Fixed by adding `TrackerIssue.description`, threading it through both `FileTracker` and `LinearAdapter::query_by_label`/`query_similar` (the GraphQL queries were missing the `description` field entirely), and building `worker::dispatch_prompt` to render title-as-heading + description instead of the bare title.
2. **`FileTracker::set_decision_state` never remapped `labels`**, only `decision_state` — and `query_by_label` filters on `labels`. Approving a local-backend issue left it invisible to the dispatch loop forever; the file backend's approval path was silently broken. `LinearAdapter` already did this correctly (add/remove `proposal:*` labels), so the two backends disagreed on the decision surface. Fixed to mirror `LinearAdapter`'s behavior.
3. **Nothing ever verified a "successful" dispatch, and nothing ever closed the tracker item.** "Success" was purely the harness's own `is_error`/exit-code self-report; `acceptance_criteria` was dead data nothing read back; and `DecisionState` had no terminal state at all — a fully successful run left the issue labeled `proposal:approved` forever, with only a comment (the trace-link note) as a signal a human had to go find. Designed and built the fix as [Worker completion](architecture/worker-completion.md): a `CompletionMode` (`None` default / `Commit` — harness commits its own work under an appended prompt instruction, lucid only observes `HEAD` before/after since there's no worktree isolation yet to make a blind `git add -A` safe) and a `ReviewMode` (`Auto` default — daemon sets `DecisionState::Done` itself off the harness's self-report; `Human` — sets a new `DecisionState::NeedsReview` and stops, the first real implementation of the "parked state" rule `docs/FEATURES.md` had flagged as unbuilt; `Agent` — a second read-only dispatch reviews the diff against `acceptance_criteria` and returns a `VERDICT: PASS`/`FAIL` line, fail-open to `NeedsReview` on anything unparseable). Cross-linked into [Review/rework UX](architecture/review-rework-ux.md): that fork assumes a PR exists to act on, which `CompletionMode::Commit` doesn't have — `ReviewMode` is the pre-merge gate instead for that mode, not a replacement for the fork once a `BranchAndPr` mode eventually exists.

Verified live end-to-end, not just via unit tests: `examples/e2e_smoke.rs` rewritten to file a real `Proposal` (with description + `acceptance_criteria` + `ReviewMode::Auto`), approve it, dispatch through the same `query_by_label("proposal:approved")` → `dispatch_prompt` → `run_dispatch` → `finalize_completion` path the daemon uses, against a real `claude -p` subscription session in `CompletionMode::Commit`. Result: the harness read the description, wrote `NOTES.md`, committed it with `LOCAL-1` in the message, lucid observed the new `HEAD` and reported "Committed `45882dfe3eaf`," and `finalize_completion` correctly set `decision_state: Done` — the whole loop closing itself with no human step after approval. `cargo build`/`cargo test` (45 passing)/`cargo clippy --all-targets` all clean.

## [2026-08-17] query | `lucid task` CLI: does a manual approve/dispatch path drift from the tracker as source of truth?

Raised directly in conversation: adding a CLI to approve/dispatch tracker items risked reading as lucid becoming a second, competing source of truth alongside the configured tracker (Linear, or the local file store). Resolved as no drift, with the distinction actually mattering split three ways, not one:

- **`lucid task list`** — pure read via `TrackerAdapter::query_by_label`. Can't drift; it's a rendering of the tracker's current state, nothing more.
- **`lucid task approve`/`reject`** — calls `TrackerAdapter::set_decision_state`, the identical call the tracker's own UI action makes (for Linear: the same `issueAddLabel`/`issueRemoveLabel` mutations a human clicking the label dropdown would trigger). This *converges* with the tracker rather than drifting from it — there's one state, and this is just another writer of it, the same way `gh issue close` doesn't compete with GitHub's own UI as a source of truth.
- **`lucid task dispatch-now <id>`** — the one case where drift was a real risk, avoided by requiring the target issue already be `Approved` in the tracker (errors out otherwise, pointing at `lucid task approve`). It changes *when* approved work runs — immediately, instead of waiting for the next presence-gated tick — never *whether* it's allowed to. Same authority check the daemon's own tick loop already does, just invoked on demand instead of on a timer.

Built accordingly: extracted `worker::dispatch_and_finalize` (build prompt → `run_dispatch` → `finalize_completion`) so `daemon::dispatch_approved_issues` and `lucid task dispatch-now` share the *exact* code path rather than two implementations that could drift from each other over time. Added `lucid task list/approve/reject/dispatch-now` (`src/cli.rs`, handlers in `src/main.rs`) — no `--review` override on approve; `ReviewMode` is still only set at proposal-filing time, not currently updatable from the CLI. Live-tested against the file backend end-to-end: seeded a pending issue, `list` (empty for `approved`), `approve` (now visible), `dispatch-now` under `CompletionMode::Commit` — harness wrote the file, committed it, `finalize_completion` marked it `Done`, all from three CLI calls with no hand-edited JSON. Documented in `docs/CLI.md` (new `lucid task` section) and cross-linked from `docs/wiki/architecture/worker-completion.md`. `cargo build`/`cargo test` (45 passing, unchanged — no new tests added by this change)/`cargo clippy --all-targets` all clean.

Also brought up Arize Phoenix locally via `docker compose up -d` (operational, not a design decision — no wiki page) to watch trace output while testing; confirmed the OTLP endpoint reachable and a dispatch's trace-link URL resolves against it.

## [2026-08-17] query | Traces were never actually exporting — two separate bugs, one environmental, one real

Follow-up to the previous entry's "confirmed... trace-link URL resolves against it": resolving is not the same as containing the right data, and pushing further surfaced two distinct problems.

**Bug 1, environmental, not lucid's fault:** a stray host-level process — `uv tool run arize-phoenix serve --host 127.0.0.1 --port 6006`, started two days earlier for unrelated work — was already bound to port 6006. `docker compose up -d` for the `phoenix` service reported success without erroring (Docker doesn't reliably detect a bare host socket the way it detects another container's port claim), but `localhost:6006` kept resolving to the stray process's own unrelated projects (`evals-sandbox`, `evals-naive`, etc.) instead of the new container — confirmed by querying the container's own live SQLite db directly (`docker exec lucid-phoenix-1 python3 -c "import sqlite3; ..."` against `/mnt/data/phoenix.db`, which showed only an empty `default` project) versus what `localhost:6006` served (the unrelated projects). Fixed by killing the stray process and `docker compose restart phoenix` so the container's own port binding took effect. Documented as a gotcha on [Trace correlation](architecture/trace-correlation.md).

**Bug 2, real, in `harness::apply_telemetry` (`src/harness/mod.rs`):** even with the port fixed, a fresh dispatch produced 0 spans and 0 traces. Ground-truthed against [Claude Code's own OTel docs](https://code.claude.com/docs/en/monitoring-usage) (`research-first`, since this depended on Claude Code's current telemetry config surface, not something to trust from memory): `CLAUDE_CODE_ENABLE_TELEMETRY=1` + `OTEL_LOGS_EXPORTER=otlp` only exports the *logs* signal — distributed tracing (spans) is a separate, off-by-default beta feature requiring both `CLAUDE_CODE_ENHANCED_TELEMETRY_BETA=1` and `OTEL_TRACES_EXPORTER=otlp`, neither of which `apply_telemetry` had ever set. This means [Trace correlation](architecture/trace-correlation.md)'s whole mechanism — the trace-link posted back to every tracker item — had never actually had spans behind it to link to, since the feature was designed. Added both missing env vars; re-ran the same dispatch and got 2 spans / 1 trace, with the span's `lucid` resource attribute (`{dispatch_id: ..., ticket_id: ...}`) confirmed to exactly match the `dispatch_id` already posted in the tracker note. Trace correlation is now verified working end-to-end for Claude Code specifically — Codex's export completeness was not re-checked and stays designed-not-confirmed, noted on the page.

`cargo test` still 45 passing (no new tests added — this was an env-var fix with no new branch to unit-test; verification was live, against a real Phoenix instance), `cargo clippy --all-targets` clean.

## [2026-08-17] query | Commit observation was single-SHA (dropped multi-commit work); direct-commit-to-shared-directory named as an anti-pattern by current practice

Prompted directly by a question about how `CompletionMode::Commit` actually works and what happens if the harness wants to make several commits — the honest answer at the time was "it doesn't handle that." `worker::describe_commit_result` compared `git rev-parse HEAD` before/after dispatch and reported a single SHA; if the harness made two or three commits (one per logical change), only the last one's SHA would ever show up in the tracker note, with no count and no indication multiple commits happened at all.

Ran a `research-first` pass to ground how autonomous coding agents actually complete tasks in git before redesigning around a guess. Findings, cited to their actual sources rather than asserted from memory:

- **Aider auto-commits after every file edit by default** (<https://aider.chat/docs/git.html>, fetched and read directly) — specifically so `/undo` can cleanly revert one step at a time. Multiple commits per task is the *normal* case for at least one widely-used agentic coding tool, not an edge case worth deprioritizing.
- **Orchestrators track agent commits by listing everything ahead of a base ref** (`git log <base>..HEAD --oneline`), not a single before/after SHA compare — the exact mechanism the old `describe_commit_result` was missing.
- **"Never let an agent commit to a shared branch — every task gets its own feature branch/worktree"** turned up as a converging rule across GitHub Copilot coding agent, Cursor background agents, and multi-agent orchestrator write-ups (buildmvpfast, addyosmani.com's "Code Agent Orchestra") — search-result-level corroboration, not deep-fetched primary sources the way the Aider docs were, and flagged as such rather than overstated.

Read together, this reframed `CompletionMode::Commit`'s no-worktree, shared-`daemon.workdir`, direct-commit design: it's not just "a smaller version of branch+PR," it's the specific thing current practice names as unsafe, independent of whether a PR happens afterward. lucid's human-approves-every-task bootstrap phase substitutes for PR *review*, but not for worktree *isolation* — two dispatches, or a dispatch overlapping local edits, can still collide in the same directory. Documented as a deliberate, now-explicit tradeoff (not an oversight) on [Worker completion](architecture/worker-completion.md), which also raises the practical priority of per-issue worktree isolation in `docs/FEATURES.md`.

Fixed the actual bug: added `worker::commits_since` (`git log --oneline <before>..HEAD`), rewrote `describe_commit_result` to report every commit instead of one SHA, and loosened `dispatch_prompt`'s instruction from "commit your changes" to explicitly "one commit or several, whatever's logically right for the change." Added a new test (`commit_mode_reports_every_commit_not_just_the_last`) exercising a two-commit dispatch and asserting both commits' messages show up in the tracker note; updated the existing single-commit test's assertion to match the new "N commit(s):" format. `cargo test`: 46 passing. `cargo clippy --all-targets`: clean.

## [2026-08-17] query | Deterministic `verify_cmd` gate for `ReviewMode::Agent`

Follow-on design question, prompted directly by the user: "how does the agent signal it's done — does that just rely on the agent following the prompt?" Traced through what's actually structural vs. prompt-trust today: the *process-ended-without-erroring* signal (Claude Code's `stream-json` `result` event's `is_error`) is real and not gameable by omission, but *task correctness* — whether a commit happened, whether `ReviewMode::Agent`'s `VERDICT:` line reflects reality — was entirely prompt-trust, mitigated only by failing open to `NeedsReview` rather than defaulting to `Done`. The user then asked specifically how a set of required deterministic checks would be configured without becoming tedious per-repo/per-task setup, which is what this entry resolves.

Decision, made with the user before implementing: lucid runs the verify command itself and checks the real exit code, rather than widening the review agent's tool access and trusting its own report of whether tests passed — an exit code is a fact, an LLM's summary of one is a claim. Config-tedium was resolved by making `verify_cmd` optional rather than required: unset, the review agent infers its own command from the repo's conventions (`Cargo.toml`/`package.json`/`CLAUDE.md`) the way a human contributor would; set, it pins an exact command. Storage reuses the existing frontmatter surface rather than adding new tracker-adapter plumbing: `verify_cmd` can't be a label the way `review` is (free text, not a small enum), so it's read back via a new `tracker::frontmatter_field(description, key)` parser — a narrow, explicit exception to "lucid doesn't re-parse the frontmatter text," documented on both [Agent handoff](architecture/agent-handoff.md) and [Worker completion](architecture/worker-completion.md).

Implementation: `Proposal.verify_cmd: Option<String>`, rendered into `render_description`'s frontmatter; `worker::run_verify_cmd` runs it via `sh -c` with a timeout and returns the exit status; wired into `finalize_completion`'s `ReviewMode::Agent` branch ahead of `agent_review` — a failing or unspawnable verify command short-circuits straight to `NeedsReview` with a note, and the LLM review dispatch never runs at all. Added 4 `frontmatter_field` unit tests and 2 `finalize_completion` tests covering both the short-circuit and pass-through paths (the short-circuit test uses empty harness profiles deliberately — if verify_cmd's failure hadn't actually prevented the review dispatch, the call would hit `DispatchError::NoProfiles` and return `Err` instead of the asserted `Ok`).

Live-verified against a real `claude -p` dispatch, not just unit tests: a failing `verify_cmd` (`exit 1`) correctly parked the issue at `NeedsReview` without ever invoking the review agent; a passing one (`test -f PING.txt`) correctly proceeded to a real review dispatch. That live run also surfaced a genuine, worth-noting characteristic rather than a bug: the reviewer's restricted `--allowedTools` (`Bash(git diff *)`/`log *`/`status *` only) `permission_denied`-blocked a compound Bash command the model tried, which ended that turn without a parseable `VERDICT:` line — correctly routed to `NeedsReview`, not a false `Done`. Rerunning the identical prompt manually produced a clean `VERDICT: PASS` immediately after, consistent with ordinary model non-determinism rather than a systemic issue with the gate itself. `cargo test`: 52 passing. `cargo clippy --all-targets`: clean.

## [2026-08-17] query | `verify_cmd` resolved through three tiers, not just per-task; grounded against Symphony and Copilot

Two rounds of user pushback on the `verify_cmd` design above, both real gaps:

1. "Wouldn't per-task `verify_cmd` mean writing the same command into every proposal?" — for most repos it's one command, true for every task (`cargo test`), so per-task-only was pushing config to the wrong layer. Fixed by adding `daemon.verify_cmd: Option<String>` in `lucid.toml` (`DaemonConfig`) as a repo-wide default — set once, same shape as a CI config.
2. "Would this mean maintaining a separate set of verification, or can it just execute existing commands? CI reuse only works for PRs, which lucid doesn't have" — correct on both counts: lucid has no worktree/branch/PR flow yet, so literally reusing `.github/workflows/*.yml` the way GitHub Copilot coding agent does isn't buildable today. Fixed by adding `worker::detect_verify_cmd`, an auto-detection tier that reads the repo's own already-existing convention instead of requiring any new config: `Cargo.toml` present → `cargo test`; `package.json` with a `scripts.test` entry → `npm test` (the project's own defined script, not a guess); `Makefile` with a `test:` target → `make test`.

`worker::resolve_verify_cmd` now checks, in order: task-level frontmatter override → `daemon.verify_cmd` → auto-detected convention → `None` (review agent infers). The common repo needs zero config at either explicit tier.

Grounded with a `research-first` pass before implementing, fetching primary sources rather than trusting summaries: OpenAI Symphony's actual `SPEC.md` (fetched and grepped directly) has **no verification mechanism at all** — the `WORKFLOW.md` frontmatter schema (§5.3: `tracker`/`polling`/`workspace`/`hooks`/`agent`/`codex`, no test/verify/build field), §2.2 explicitly scopes "business logic for tickets/PRs" out as a non-goal delegated to the workflow prompt, and its own success criterion is soft (§11.5: "a successful run can end at a workflow-defined handoff state... not necessarily Done"). Its closest structural relative, `hooks.after_run`, is a repo-level shell-script string in `WORKFLOW.md` — same shape as `daemon.verify_cmd` — but its own spec says failures there are "logged but ignored," not a gate. GitHub Copilot coding agent (from search results, not a primary-source fetch — lower confidence than the Symphony citation) reuses the repo's actual existing CI, triggered on the PR it opens, gated behind human approval to run plus mandatory human review before merge. Net: lucid's `verify_cmd` gate is already more rigorous than the most-cited prior art (Symphony has none), and its shape — run the same command CI would run, directly, since there's no PR to trigger CI against yet — is the honest analog to Copilot's approach given what lucid can build today, not a deviation from an established pattern.

Added 5 new tests (`resolve_verify_cmd_*`) covering the three-tier precedence and each auto-detection branch. `cargo test`: 57 passing. `cargo clippy --all-targets`: clean.

## [2026-08-17] query | Removed `verify_cmd` auto-detection — a guessed check is worse than no check

Immediate follow-up pushback on the auto-detection tier added in the entry above, right after it landed: guessing a command from repo shape (`Cargo.toml` → `cargo test`) risks silently narrowing "verified" to whatever that one guess covers, while the repo's real CI might also lint, typecheck, or run several separate suites — a partial check that presents itself as a real gate is worse than no gate, since it creates false confidence instead of an honest "nothing was checked."

Removed `worker::detect_verify_cmd` entirely. `worker::resolve_verify_cmd` now resolves through exactly two tiers — task-level `Proposal.verify_cmd` override, then `daemon.verify_cmd` repo default — with no fallback beyond that; unset at both means `verify_cmd` doesn't run at all, same as before this gate existed (the review agent infers its own command, an honestly weaker LLM judgment, not a narrower deterministic one). Updated `docs/wiki/architecture/worker-completion.md` and `docs/FEATURES.md` in place to match (both describe current behavior, not history, so they were corrected directly rather than appended to). The Symphony/Copilot grounding research from the entry above still stands — it grounded the *shape* of config-driven verification, not the now-removed auto-detection specifically. Removed 3 auto-detection tests, kept/renamed the 2 tier-precedence tests. `cargo test`: 55 passing. `cargo clippy --all-targets`: clean.

## [2026-08-17] ingest | `lucid task create` — a real write path for `Proposal.review`/`verify_cmd`

Every field `worker-completion.md` documents (`review`, `verify_cmd`, `acceptance_criteria`) had a defined shape but no ergonomic way to set it: filing a proposal meant hand-editing a ticket's frontmatter text or constructing a `Proposal` struct directly in Rust (`examples/e2e_smoke.rs`). Added `lucid task create <title>` (`src/cli.rs`: `TaskCommand::Create`, `CliEffort`, `CliReviewMode`; `src/main.rs`: `task_create`), a thin CLI call through `TrackerAdapter::create_proposal` — the same write path `pm::wake` uses, minus its `query_similar` dedup check (a human typing a title explicitly isn't the runaway re-filing case that guards against). Prints the new issue id on success (`LOCAL-1` for the file backend), so the next command (`lucid task approve <id>`) has something to act on.

Live-verified the full round trip against the file backend, not just unit tests: `lucid task create --verify-cmd "exit 1" --review agent` → `lucid task approve` → `lucid task dispatch-now` correctly parked the issue at `NeedsReview` with a "Verify command `exit 1` failed (nonzero exit)" note — confirming the write (`render_description`) → read-back (`tracker::frontmatter_field`) → resolve (`worker::resolve_verify_cmd`) path holds together end to end through the new command, the same class of check that caught the description-passthrough and label-remap bugs earlier in this project.

Updated `docs/CLI.md` (new command + flag table), `docs/FEATURES.md`, and `docs/wiki/architecture/worker-completion.md` (task-level `verify_cmd`/`review` tiers now cross-reference this as their write path; the Linear-label-prerequisite note now covers this CLI path too, since it calls the same `create_proposal`). `cargo test`: 55 passing (no new tests needed — this is CLI plumbing over an already-tested `create_proposal`). `cargo clippy --all-targets`: clean.

## [2026-08-17] query | Decision state moves the real Linear ticket state, not a label

First real setup of the Linear backend surfaced a design flaw the file-backend-only development had hidden: `DecisionState` was purely a `proposal:*` label on the Linear issue, never touching the issue's actual `state` field — invisible to Linear's board view, filters, and any state-based automation. For a system whose stated point is controlling a task through a real project tracker rather than a bespoke store, tracking state on the side in a label defeats that purpose: a human looking at the board sees nothing of lucid's decisions.

Three designs were weighed: (a) map onto Linear's built-in `type` category (`backlog`/`unstarted`/`started`/`completed`/`canceled`) for minimal setup, but `Approved` and `NeedsReview` have no native type equivalent; (b) a hybrid — real state for the four concepts that map to a `type`, label only for the two that don't; (c) six explicitly named workflow states owned by lucid (`Pending`, `Approved`, `In Review`, `Done`, `Rejected`, `Stale`), looked up by name per team, mirroring the `review:*` label setup already required. Chose (c) for symmetry — one explicit setup story instead of two different rules depending on which decision state — and because a `type`-based mapping would silently reuse whatever generic `Todo`/`Done` a team already has for unrelated human tickets, conflating lucid's own semantics with the team's.

`LinearAdapter::set_decision_state` now issues a single `issueUpdate(input: {stateId})`, replacing the old add/remove-label diff dance entirely (no multi-valued-field ambiguity to protect against, unlike labels). `create_proposal` sets `stateId` on issue creation instead of a decision label. `TrackerAdapter::query_by_label(&str)` is renamed `query_by_decision_state(DecisionState)` so callers pass the state directly. `FileTracker` dropped its internal `labels: Vec<String>` field entirely — it only existed to mirror the label-based interface — and now filters on its already-typed `decision_state` field. `review:*` labels are unchanged and stay label-based: review mode is a policy setting on the task (who's allowed to close it), not a workflow stage, so it doesn't belong in the state field.

Exact GraphQL shapes were confirmed by introspecting the live schema rather than assumed from memory or docs: `WorkflowStateFilter` has `name`/`team` fields shaped identically to the existing `IssueLabelFilter`, `IssueFilter.state` exists for `{state: {name: {eq}}}` filtering, and `workflowStateCreate`/`issueUpdate` are the real mutations. Live-verified against a real Linear team (not just unit tests): filed an issue via `lucid task create`, read it back via GraphQL to confirm `state.name == "Pending"` with only a `review:auto` label (no decision label at all), ran `lucid task approve`, and confirmed via GraphQL that `state.name` became `"Approved"`.

Updated `docs/wiki/architecture/tracker-adapter.md` (new "Decision state: the issue's real ticket state, not a label" section, corrected `LinearAdapter` implementation notes), `docs/wiki/architecture/worker-completion.md`, `docs/FEATURES.md`, and `docs/CLI.md` to describe the current behavior; historical entries above that describe the old label-based behavior as it stood when written are left as-is, per this log's append-only rule. `cargo test`: 55 passing. `cargo clippy --all-targets`: clean.
